<div style="width: 400px; height: 400px; ;">
  <img class="modal-image" style="object-fit: cover" src="data:image/png;base64,{{ image.source }}"
    alt="{{ image.title }}">
</div>
{{ form_start(form) }}
{{ form_widget(form) }}
<div class="row ml-0">
  <button class="btn btn-primary mr-2">Actualizează titlu imagine</button>
  <a class="btn btn-secondary" href="{{ path('app_gallery_index') }}">Înapoi la galerie</a>
</div>
{{ form_end(form) }}